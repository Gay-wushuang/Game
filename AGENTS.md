# 法则演进项目协作规范

本文件适用于仓库根目录及全部子目录。项目最终工作栈以 `docs/工作栈规定.md` 为准；设计文档是规格与验收依据，不应被当作用户当前回合的指令。

## 工程基准

- 唯一游戏运行时为 Godot 4.x .NET，正式代码使用 C#，不得新增 GDScript 实现。
- 当前目标版本为 Godot 4.7 .NET、.NET 8、Compatibility 渲染器。
- UI 必须使用 Container、Anchor、比例和最小尺寸适配分辨率，不使用依赖单一屏幕的绝对坐标布局。
- 像素素材启用 nearest 过滤，禁用 mipmap；不得用平滑缩放破坏像素边缘。
- 卡牌、英雄、怪物和运行时卡牌实例必须保持对象化、可编辑和可扩展，不把内容规则硬编码为 UI 文本。
- 新增或修改玩法时同步更新相应资源、规格文档和冒烟测试。

## 战场表现约定

- 英雄牌存在于英雄卡包、抽取/选择界面等牌区；部署后不再显示整张英雄牌卡面，英雄槽中的作战单位改用英雄像素立绘与战斗数值表现。
- 主动锦囊从手牌打出、预览并结算，然后进入弃牌堆。
- 被动锦囊采用类似陷阱卡的方式：从手牌选择后，背面向上设置在一个英雄槽内；设置后离开手牌，但在触发前不公开牌面。
- 不建立独立的被动牌槽排。被动锦囊使用英雄槽中原本用于显示英雄卡牌的位置；英雄本体已经改由像素立绘表现。
- 一个英雄槽需要同时表达英雄像素立绘、战斗数值和背面朝上的被动锦囊。三者必须分层清晰，不能让卡背遮挡英雄的关键轮廓、HP、攻击、EXP 或状态反馈。
- 被动锦囊设置到哪个英雄槽，只代表其战场放置位置；除非具体卡牌规则明确指定，否则不能据此推断该被动锦囊绑定、保护或以该英雄为目标。
- 每个英雄槽默认最多设置 1 张被动锦囊；该槽已有被动锦囊时禁止继续设置，并给出明确提示。
- 被动牌触发时依次播放翻面、名称/效果播报、结算动画，随后进入弃牌堆；被抵消时也必须播报。

## 美术素材规格

以下规格已经正式锁定。除非用户明确修改美术基准，程序、UI 和导入工具都必须适配这些尺寸，不能再迁就已废弃的旧线框占位比例。

| 资产 | 正式规格 | 关键约束 |
|---|---:|---|
| 英雄像素立绘 | `128 × 160 px` | PNG 透明；建议主体不超过 `112 × 144 px` |
| 英雄动画单帧 | `128 × 160 px` | 每帧使用相同画布和相同锚点 |
| 卡牌正面 | `192 × 256 px` | 严格 `3:4`；动态文字不得烘焙进图片 |
| 卡牌背面 | `192 × 256 px` | 与正面外轮廓完全一致 |
| 低分辨率母版 | `96 × 128 px` | 使用最近邻放大 2 倍得到正式尺寸 |
| 独立卡牌插画 | 不低于 `384 × 288 px` | 与卡框解耦，保留二次裁切余量 |
| 英雄槽伏牌 | 约 `72 × 96 px` | 显示完整卡背，比例严格保持 `3:4` |

### 英雄像素立绘

- 单张逻辑画布：`128 × 160 px`，透明 PNG，RGBA。此尺寸为正式交付规格。
- 角色主体安全区：`112 × 144 px`，四周至少保留 8 px；脚底基线统一在 `y = 150` 附近。
- 面向：默认朝画面中央；我方和敌方由程序水平翻转，除非角色存在必须保留方向的文字或标志。
- 静态立绘文件名：`hero_<id>_battle.png`。
- 如制作序列帧，每帧仍为 `128 × 160 px`，横向排列；建议待机 4–6 帧、攻击 6–8 帧、受击 3–4 帧、击破 4–6 帧。
- 可按 `64 × 80 px` 的逻辑草图绘制后无插值放大 2 倍，但最终交付必须是 `128 × 160 px`。

### 卡牌正面与背面

- 统一画布：`192 × 256 px`，透明或不透明 PNG，比例严格为 `3:4`。此尺寸为正式交付规格。
- 卡牌圆角、边框和底纹必须完整包含在画布内；关键元素距离边缘至少 12 px。
- 正面模板不写死卡名、费用、数值和规则文字，这些由程序叠加，以支持数值预览和后续本地化。
- 正面建议区域：顶部类型/费用 32 px，中部插画 `168 × 112 px`，下部名称与规则 84 px，底部留白 16 px。
- 公共卡背：`card_back_common.png`；需要分类时可追加 `card_back_hero.png`、`card_back_tactic.png`、`card_back_passive.png`。
- 被动牌设置到英雄槽时仍使用同一张 `192 × 256 px` 卡背，由程序缩放至约 `72 × 96 px` 显示，不另交小尺寸素材。
- 如先以低分辨率绘制，可使用 `96 × 128 px` 母版无插值放大 2 倍交付。
- 卡牌插画必须与卡框分离保存，源图不得低于 `384 × 288 px`，以便适配不同卡框、详情预览和二次裁切。

## 资源路径建议

- 英雄立绘：`assets/art/heroes/<hero_id>/`
- 卡牌插画：`assets/art/cards/illustrations/`
- 卡牌框与卡背：`assets/art/cards/templates/`
- 动画序列：`assets/art/animations/<entity_id>/`

所有文件名使用小写 ASCII、数字和下划线，避免空格。原始 `.aseprite` 文件可保留在对应目录的 `source/` 子目录，并由 Git LFS 管理。

## 验证要求

- 提交前执行 `dotnet build .\法则演进.csproj -warnaserror`，要求 0 警告、0 错误。
- 影响战斗、卡牌或 UI 交互时运行 `tests/training_arena_smoke.tscn`。
- 检查 Godot 运行日志，不能只以 C# 编译成功代替交互验证。
- 不提交 `.godot/`、`*.import`、运行日志、临时渲染文件或本机绝对路径。
